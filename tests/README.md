# UniFound Test Suites

This directory hosts system integration tests and end-to-end testing runners across all modules.

- `backend/tests/`: Unit & integration tests for FastAPI, auth, models, and endpoints.
- `frontend/`: Vitest / React testing library specs.
- `tests/`: End-to-end integration and MCP verification suites.
